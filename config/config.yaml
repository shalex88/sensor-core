app:
  name: sensor-core
  log_level: debug
  api:
    api_type: rest
    port: 3000
  infrastructure:
    clients:
      camera_service:
        instances:
          - id: 0 # nfov
            server: frontier-peripheral-ctrl-mpsoc.local
            port: 50050
          - id: 1 # wfov
            server: frontier-peripheral-ctrl-mpsoc.local
            port: 50051
          - id: 2 # mwir
            server: frontier-peripheral-ctrl-mpsoc.local
            port: 50052
          - id: 3 # swir
            server: frontier-peripheral-ctrl-mpsoc.local
            port: 50053
      video_service:
        instances:
          - id: 0 # nfov
            server: localhost
            port: 50060
          - id: 1 # wfov
            server: localhost
            port: 50061
          - id: 2 # mwir
            server: localhost
            port: 50062
          - id: 3 # swir
            server: localhost
            port: 50063